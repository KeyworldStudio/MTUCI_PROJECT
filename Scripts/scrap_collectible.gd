extends RigidBody2D

@export var nominal: float = 1
#
#func _physics_process(delta):
	#
	#linear_velocity = linear_velocity.move_toward(Vector2.ZERO, 10*linear_velocity.length()*delta)

	#move_and_slide()
	#if velocity == Vector2.ZERO:
		#disable_mode = DI
